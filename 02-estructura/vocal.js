let vocal = 'i';

if(vocal === 'a' || vocal === 'A'){
    console.log(`la letra ${vocal} es una vocal`);
}else if(vocal === 'e' || vocal === 'e'){
    console.log(`la letra ${vocal} es una vocal`);
}else if(vocal === 'i' || vocal === 'I'){
    console.log(`la letra ${vocal} es una vocal`);
}else if(vocal === 'o' || vocal === 'O'){
    console.log(`la letra ${vocal} es una vocal`);
}else if(vocal === 'u' || vocal === 'U'){
    console.log(`la letra ${vocal} es una vocal`);
}else{
    console.log(`el valor ${vocal} ingresado no es vocal`)
}